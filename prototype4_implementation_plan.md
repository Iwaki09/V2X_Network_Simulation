# SUMOベースV2X通信シミュレーション - 詳細実装計画

## プロジェクト概要

SUMOを使った交通シミュレーションと、車両間通信（V2V）のグラフ最適化を組み合わせたシミュレーションアプリケーションの実装。

### 主な機能
1. SUMOによる6台の車両の交通シミュレーション
2. 車両位置情報の取得とグラフ化
3. 車両間通信チャネルモデルの定義
4. 通信品質に基づくグラフ最適化（最適経路計算）
5. matplotlibによる可視化

---

## Phase 1: 環境準備とディレクトリ構成

### 1.1 ディレクトリ構造作成
- [ ] `prototype4/` ディレクトリを作成
- [ ] `prototype4/sumo_scenarios/` サブディレクトリを作成
- [ ] `prototype4/output/` サブディレクトリを作成（シミュレーション結果保存用）
- [ ] `prototype4/figures/` サブディレクトリを作成（可視化結果保存用）

### 1.2 依存パッケージ確認
- [ ] SUMO TraCIライブラリのインストール確認（`traci`, `sumolib`）
- [ ] NetworkXのインストール確認
- [ ] matplotlibのインストール確認
- [ ] requirements.txtに追記（必要な場合）

---

## Phase 2: SUMOシナリオファイル作成

### 2.1 道路ネットワーク定義 (`network.net.xml`)
- [ ] 簡単な直線道路（長さ1000m程度）を定義
- [ ] 1車線または2車線の道路として設定
- [ ] エッジ（道路区間）とノード（交差点/端点）を定義
- [ ] `netgenerate`コマンドまたは手動XMLで作成

### 2.2 車両ルート定義 (`routes.rou.xml`)
- [ ] 車両タイプを定義（乗用車、速度範囲など）
- [ ] 6台の車両を定義
  - [ ] 車両1: 開始時刻=0s, 初期位置=0m, 速度=15m/s
  - [ ] 車両2: 開始時刻=2s, 初期位置=50m, 速度=20m/s
  - [ ] 車両3: 開始時刻=5s, 初期位置=100m, 速度=18m/s
  - [ ] 車両4: 開始時刻=3s, 初期位置=150m, 速度=12m/s
  - [ ] 車両5: 開始時刻=7s, 初期位置=200m, 速度=22m/s
  - [ ] 車両6: 開始時刻=1s, 初期位置=80m, 速度=16m/s
- [ ] 各車両のルート（走行経路）を設定

### 2.3 SUMO設定ファイル (`config.sumocfg`)
- [ ] ネットワークファイルとルートファイルのパスを指定
- [ ] シミュレーション時間を設定（例: 100秒）
- [ ] タイムステップを設定（例: 0.1秒）
- [ ] 出力オプションを設定

### 2.4 実行スクリプト (`run_sumo.sh`)
- [ ] SUMOをコマンドラインで実行するシェルスクリプトを作成
- [ ] TraCIポートを指定（Pythonから接続するため）
- [ ] GUIあり/なしのオプションを用意

---

## Phase 3: SUMO-Python統合

### 3.1 基本的なTraCI接続 (`sumo_simulation.py`)
- [ ] TraCIを使ってSUMOに接続する関数を実装
- [ ] SUMOシミュレーションを開始する関数を実装
- [ ] シミュレーションステップを進める関数を実装
- [ ] SUMOを終了する関数を実装

### 3.2 車両情報取得
- [ ] 各タイムステップで車両IDリストを取得
- [ ] 各車両の位置（x, y座標）を取得
- [ ] 各車両の速度を取得
- [ ] 各車両の角度（進行方向）を取得

### 3.3 データ構造定義
- [ ] 車両情報を格納するクラスまたは辞書を定義
- [ ] タイムステップごとの車両情報を保存するリストを用意
- [ ] ログ出力機能を実装（デバッグ用）

---

## Phase 4: 通信チャネルモデル

### 4.1 基本パラメータ定義 (`channel_model.py`)
- [ ] 周波数（例: 5.9GHz - V2X専用）
- [ ] 送信電力（例: 20dBm）
- [ ] アンテナゲイン（送信・受信）
- [ ] ノイズフロア（例: -95dBm）
- [ ] 最大通信距離（例: 300m）

### 4.2 パスロスモデル実装
- [ ] 自由空間パスロスの計算関数
- [ ] 対数距離パスロスモデルの実装
- [ ] パスロス指数（α）の設定（例: 2.5）

### 4.3 通信品質計算
- [ ] 受信電力の計算関数
- [ ] SNR（信号対雑音比）の計算関数
- [ ] データレート推定関数（Shannon容量の簡易版）
- [ ] 通信可否判定関数（距離閾値またはSNR閾値）

### 4.4 車両間チャネル行列生成
- [ ] N×N行列（N=車両数）を生成する関数
- [ ] 各エントリにチャネル品質（SNR, データレート）を格納
- [ ] 通信不可の場合は0または無限大を設定

---

## Phase 5: グラフ化と最適化

### 5.1 グラフ構築 (`graph_optimizer.py`)
- [ ] NetworkXのGraphオブジェクトを作成
- [ ] 車両をノードとして追加
- [ ] 車両間に通信可能なリンクをエッジとして追加
- [ ] エッジの重み付け（通信品質の逆数、または距離）

### 5.2 経路探索アルゴリズム実装
- [ ] Dijkstra法による最短経路計算
- [ ] 始点・終点を指定できるようにする
- [ ] すべてのペア間の最短経路を計算する機能（オプション）
- [ ] 経路が存在しない場合のハンドリング

### 5.3 通信品質評価
- [ ] 選択された経路の総コストを計算
- [ ] 経路上の最悪リンク品質を評価
- [ ] ホップ数のカウント

### 5.4 最適化基準の実装
- [ ] 最短距離経路
- [ ] 最高品質経路（最大データレート）
- [ ] 最小遅延経路

---

## Phase 6: 可視化

### 6.1 静的可視化 (`visualizer.py`)
- [ ] 特定タイムステップの車両位置をプロット
- [ ] 車両間の通信リンクを線で表示
- [ ] リンク品質に応じて線の色・太さを変更
- [ ] 計算された最適経路をハイライト表示

### 6.2 動的可視化（アニメーション）
- [ ] matplotlibのFuncAnimationを使用
- [ ] タイムステップごとに車両位置を更新
- [ ] リンクを動的に更新
- [ ] 経路をリアルタイムで再計算・表示

### 6.3 グラフ可視化
- [ ] NetworkXのdraw機能で通信グラフを描画
- [ ] ノードラベルに車両IDを表示
- [ ] エッジラベルに通信品質を表示
- [ ] レイアウトアルゴリズムを選択（spring_layout等）

### 6.4 統計情報の表示
- [ ] 平均通信品質の時系列グラフ
- [ ] 接続されている車両ペア数の推移
- [ ] 経路コストの推移

---

## Phase 7: 統合とテスト

### 7.1 メインシミュレーションループ
- [ ] SUMOシミュレーション、チャネル計算、グラフ最適化を統合
- [ ] 各タイムステップでの処理フローを実装
  1. SUMOから車両情報取得
  2. チャネル行列計算
  3. グラフ構築
  4. 最適経路計算
  5. 結果を保存
- [ ] 全タイムステップをループ処理

### 7.2 出力データ保存
- [ ] 車両軌跡をCSVで保存
- [ ] チャネル行列の時系列をNumPy配列で保存
- [ ] 最適経路情報をJSONで保存

### 7.3 テストとデバッグ
- [ ] 小規模シナリオ（2-3台）でテスト
- [ ] ログ出力を確認
- [ ] 可視化結果の妥当性確認
- [ ] 6台フルスケールでのテスト実行

### 7.4 ドキュメント作成
- [ ] README.md作成（実行方法、パラメータ説明）
- [ ] コードにdocstringを追加
- [ ] 使用例のサンプルコード作成

---

## ディレクトリ構造（完成予定）

```
prototype4/
├── sumo_scenarios/
│   ├── network.net.xml      # 道路ネットワーク定義
│   ├── routes.rou.xml       # 車両ルート定義（6台）
│   └── config.sumocfg       # SUMO設定ファイル
├── output/                  # シミュレーション結果出力
│   ├── vehicle_traces.csv
│   ├── channel_matrices.npy
│   └── optimal_paths.json
├── figures/                 # 可視化結果保存
│   ├── snapshot_t10.png
│   ├── animation.mp4
│   └── statistics.png
├── run_sumo.sh             # SUMOシミュレーション実行スクリプト
├── sumo_simulation.py      # SUMOとPythonの統合メインスクリプト
├── channel_model.py        # 通信チャネルモデル定義
├── graph_optimizer.py      # グラフ最適化（最短経路計算）
├── visualizer.py           # matplotlib可視化
└── README.md               # 実行方法とドキュメント
```

---

## 技術スタック

- **SUMO (Simulation of Urban MObility)**: 交通シミュレーション
- **TraCI (Traffic Control Interface)**: PythonからSUMOを制御
- **NetworkX**: グラフ理論・最適化ライブラリ
- **matplotlib**: 可視化・アニメーション
- **NumPy**: 数値計算・行列演算

---

## 補足事項と推奨実装順序

1. **Phase 2（SUMOシナリオ）を先に完成させる**
   - SUMO単体で正しく動作することを確認
   - `sumo-gui`で視覚的に確認すると良い

2. **Phase 3でTraCI接続を確認**
   - 最初は車両位置をprint出力するだけの簡単なスクリプトで動作確認

3. **Phase 4のチャネルモデルは段階的に複雑化**
   - 最初は距離ベースの簡単なモデル
   - 後からパスロスモデルを追加

4. **Phase 6の可視化は早めに実装**
   - デバッグに非常に有用
   - 静的可視化を先に実装し、後でアニメーションを追加

5. **各Phaseごとに動作確認**
   - 一気に全部実装せず、段階的にテスト

---

## 想定される課題と対処法

| 課題 | 対処法 |
|------|--------|
| TraCI接続エラー | SUMOのバージョン確認、ポート番号の確認 |
| 車両が途中で消える | ルート定義の確認、シミュレーション時間の延長 |
| グラフが疎になる | 通信距離閾値を増やす、車両密度を上げる |
| 可視化が重い | タイムステップを間引く、フレームレートを下げる |

---

## 参考資料

- SUMO Documentation: https://sumo.dlr.de/docs/
- TraCI Python API: https://sumo.dlr.de/docs/TraCI/Interfacing_TraCI_from_Python.html
- NetworkX Documentation: https://networkx.org/documentation/stable/
- V2X通信規格: IEEE 802.11p / C-V2X

---

**作成日**: 2025-10-02
**プロジェクト**: V2X Network Simulation - Prototype 4
