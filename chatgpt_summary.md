# 研究まとめ（D/K × MCS によるアウトエージ回避最適化：交差点シナリオ）

> 本ドキュメントは、これまでの議論・実装・実験結果を、論文本文として組み立てられる粒度で文章化したものです。  
> 主軸は **(i) Ray Tracing に基づくリンク品質推定** と **(ii) D/K（伝搬モード）および MCS（離散レート）を活用したアウトエージ回避の最適化** です。  
> V2V は「リレーが成立し得る世界観」を担保するために **リレー可能率（relay-possible rate）** を参考指標として提示します（本筋は信頼性＝アウトエージ回避）。

---

## 1. 背景と課題設定

ミリ波 V2X（V2I/V2V）では、建物遮蔽や反射により **LOS/NLOS が短時間に切り替わり**、受信電力やSNRが大きく変動する。従来のシステム評価では、(a) 単純な幾何モデルや統計チャネル、(b) Shannon容量（理想連続レート）に基づく評価が多く、**環境依存の遮蔽・反射を反映した「現実寄りのリンク評価」** と、**信頼性（アウトエージ回避）を狙った制御・最適化**の間にギャップがある。

本研究は、SUMO による車両移動と Sionna RT による Ray Tracing を接続し、環境形状（建物）を反映したリンク品質時系列を生成する。さらに、Shannon容量に加えて **離散MCSテーブルに基づくスループット推定**を導入し、**伝搬状態の構造情報（D/K）**を用いて **アウトエージ（低レート状態）を避けるための最適化**を行う。

---

## 2. シミュレーション構成（パイプラインの概要）

本研究で構築した評価パイプラインは以下である。

1. **交通シミュレーション（SUMO）**  
   車両の位置・速度の時系列（FCDなど）を生成する。

2. **電波伝搬推定（Sionna Ray Tracing）**  
   建物形状と送受信ノード配置に基づき、各時刻におけるリンクの多重波情報を推定する。  
   出力には、受信電力、パスロス、遅延広がり、LOS/NLOS、パス数、各パス電力等が含まれる。

3. **リンクモデル（SNR → レート）**  
   受信電力からSNRを算出し、以下の2モデルでスループットを推定する。  
   - Shannon理論容量（連続レート：上限評価）  
   - 離散MCSモデル（8段階：現実寄り評価）

4. **最適化（リンク選択／経路選択）**  
   各時刻・各車両に対し、利用すべきリンク（例：V2I直結／V2V経由など）を選択する。  
   本研究の主眼は、平均スループット最大化ではなく **アウトエージ回避（信頼性）**である。

5. **分析（CDF/分布/アウトエージ率）**  
   LOS/NLOS、D/K条件での距離・SNR・MCS分布、さらにV2V基盤の妥当性としてリレー可能率を算出する。

---

## 3. 出力データ仕様（主要CSV）

### 3.1 theoretical_network_results.csv（主分析対象）

以下の列を含む（抜粋）。

- `timestamp`, `link_type`, `tx_id`, `rx_id`
- `is_line_of_sight`（LOS/NLOS）
- `num_paths`, `p_tot_watts`, `p_max_watts`
- `dominance`, `k_factor`, `k_factor_db`, `prop_mode`（D/K）
- `snr`, `snr_db`
- `theoretical_throughput_mbps`（Shannon）
- `mcs_index`, `spectral_efficiency_bpshz`, `throughput_mbps_mcs`（MCS）

> 注：あなたの現状の出力では、`link_quality_results.csv` は「リンク品質列の基礎（LOS/NLOSやD/Kなど）」であり、  
> `theoretical_network_results.csv` が「SNRやスループット列を含む主分析対象」として扱う。

---

## 4. 伝搬モード指標：D/K（Propagation-Mode Switch）

### 4.1 定義

Ray Tracing は複数経路（直達波・反射等）の電力を返す。各時刻リンクについて経路電力を p_i とすると、

- 総電力：  
  P_tot = Σ p_i
- 最強パス電力：  
  P_max = max p_i

ここから次の指標を定義する。

- **支配度（dominance）**  
  dominance = P_max / P_tot
- **K-factor（強成分対散乱成分比）**  
  K = P_max / (P_tot - P_max)

さらに dominance の閾値 D_TH により伝搬モードを二値分類する。

- `prop_mode = D`（dominant）：`dominance ≥ D_TH`  
- `prop_mode = K`（non-dominant / multipath）：`dominance < D_TH`

> 重要：本研究では D/K を「平均的に強い／弱い」と固定的に解釈しない。  
> D/K を **レジーム（伝搬状態の型）を識別する特徴量**として扱い、信頼性（アウトエージ）観点の設計に接続する。

---

## 5. 離散MCSモデル（SNR → MCS → スループット）

### 5.1 背景

Shannon容量は理想化された連続レートであり、実システムのリンク適応は **離散的なMCS（Modulation and Coding Scheme）** を用いる。そこで本研究では、評価の現実性を高めるために **簡略MCSテーブル（8段階）** を導入した。

### 5.2 簡略MCSテーブル（研究用）

| MCS Index | SNR閾値 [dB] | スペクトル効率 [bits/s/Hz] | 変調方式相当 |
|---:|---:|---:|---|
| 0 | < -5 | 0.15 | QPSK 1/8 |
| 1 | -5 ~ 0 | 0.38 | QPSK 1/3 |
| 2 | 0 ~ 5 | 0.88 | QPSK 2/3 |
| 3 | 5 ~ 10 | 1.48 | 16QAM 1/2 |
| 4 | 10 ~ 15 | 2.40 | 16QAM 3/4 |
| 5 | 15 ~ 20 | 3.30 | 64QAM 2/3 |
| 6 | 20 ~ 25 | 4.40 | 64QAM 5/6 |
| 7 | ≥ 25 | 5.50 | 256QAM 3/4 |

帯域幅を BW とすると、MCSベーススループットは

- R_mcs = BW · η_mcs

で与えられる（本研究では BW=100 MHz とし、最大値は 5.5×100MHz=550 Mbps で頭打ちとなる）。

---

## 6. シナリオ設計：交差点（Corner Intersection）

### 6.1 動機

単純な直線道路＋建物1棟の環境ではLOSが支配的となり、NLOSやマルチパス状態が十分に出現しない。その結果、D/Kや信頼性指標の検証に必要なサンプルが不足する。  
そこで、遮蔽イベントが頻繁に生じる **交差点（コーナー）シナリオ**を設計し、LOS/NLOS切替とマルチパス状態の出現頻度を高めた。

### 6.2 配置（例）

- 交差点中心：(0,0)  
- 道路：十字交差点（x=0 と y=0 の直線道路）
- 建物：交差点四隅に4棟（例：中心±40m、サイズ60×60m、セットバック10m）
- 基地局：(120, 120) 付近（高さ20m程度）

この構成により、交差点通過時に遮蔽の境界を横切り、LOS/NLOSが動的に切替わる状況を生成する。

---

## 7. “最小の追加分析”：距離CDF・SNR CDF・MCS分布

シナリオ内で条件別に統計を示し、後段の最適化（信頼性設計）の根拠を整える。必要最小限の分析は以下の3点である。

1. **tx-rx距離のCDF**（All/LOS/NLOS、D/K条件）  
2. **SNR(dB)のCDF**（All/LOS/NLOS、D/K条件）  
3. **MCS index 分布**（特にNLOS条件、D/K条件）

---

## 8. V2V基盤の担保：リレー可能率（Relay-Possible Rate）

### 8.1 目的

本研究の主軸は D/K×MCS による **アウトエージ回避**である。一方、V2Xの文脈ではV2Vが土台にあるため、シナリオが「V2V（将来のリレー）が成立し得る」交通密度であることを示す必要がある。そこで、実際にパケット転送を行わずとも、**“リレー可能な局面が存在する割合”**を定量化する。

### 8.2 定義（採用例）

各時刻 t、車両 v が「リレー可能」と判定される条件：

(1) v の直V2Iがアウトエージ候補：  
- R_V2I(v,t) < T_out

(2) 近傍車両 u が存在し、以下を満たす：  
- R_V2V(v→u,t) ≥ T_v2v_min  
- R_V2I(u,t) ≥ T_v2i_good

レート R には MCSベース（throughput_mbps_mcs）を用いる。

デフォルト例：
- T_out = 50 Mbps  
- T_v2v_min = 50 Mbps  
- T_v2i_good = 100 Mbps

> 注：近傍集合は「距離R以内」または「Top-K近傍」等で制限し、計算量と現実性を両立させる。

### 8.3 実験例（シナリオ調整後：あなたの実測値）

- total_samples = 4123  
- num_outage_candidates = 1235  
- num_outage_and_relay_possible = 97  

導出指標：
- outage_candidate_rate = 0.2995  
- relay_possible_rate_vehicle = 0.0235  
- conditional_relay_possible_rate = 0.0785  

交通密度（交差点周辺）：
- avg_vehicles_per_timestamp = 67.59  
- rate_vehicles_with_neighbors = 0.9966  

解釈：
- 「救済対象（アウトエージ候補）」が約30%存在  
- そのうち約7.85%は「条件を満たす中継車」を見つけられる  
- 全体では約2.35%がリレー可能状態  
→ **V2Vが土台にある世界観**を“参考指標”として成立させるのに十分な非ゼロの値が得られた。

---

## 9. 本研究の核心：mode-aware fading margin によるアウトエージ回避

### 9.1 なぜ「最適化しても効果が見える」のか（最重要な論点）

本研究の最適化は、各時刻のリンク選択（V2I直結、V2V候補など）を決める「スケジューリング／ルーティング器」に相当する。  
パケットレベル（MAC競合、HARQ再送、バッファ等）を回していなくとも、各リンクの時系列レート（SNR→MCS→throughput）が得られていれば、

- 「その時刻にどのリンク集合を選ぶか」
- 「選んだリンク集合を前提に、どの程度のアウトエージが発生するか」

をオフラインに評価できる。

ただし、**最適化に使うレートと、評価に使うレートが同一**だと、設計の工夫（例：保守化）が「自作自演」に見える危険がある。  
したがって本研究では、

- **最適化は保守的見積もり（estimate）**で実行  
- **評価はマージン無し真値（truth）**で実行

という「入力列と評価列の分離」を採用し、最適化手法の違いが **真値評価でアウトエージ率として現れる**ように設計する。

### 9.2 Rayleighフェージングに基づく理論マージン（一般的仮定）

支配的成分がない拡散環境（Rayleighフェージング）では、瞬間SNR γ は平均SNR γ̄ に対して指数分布となり、下位 p 分位は

- γ_p = -γ̄ ln(1-p)

で与えられる。平均SNRから下位 p 分位を見込むバックオフ（マージン, dB）は

- M_Rayleigh(p) = 10 log10( 1 / (-ln(1-p)) )

本研究では例として p=0.10 を採用し、

- margin_D ≈ 9.8 dB

を Dモード（支配的成分がない＝Rayleigh寄り）へ適用する。  
一方、Kモード（支配的成分あり＝Ricean寄り）は下振れが相対的に小さいという一般的仮定に基づき、簡便に

- margin_K = 3.0 dB

を採用する（パラメータとして変更可能）。

> ポイント：この設計は「特定シナリオでたまたま観測された相関に合わせて調整」ではなく、  
> **フェージング理論に基づく一般的な“下振れを見込むリンク適応”**をD/Kによって切り替える、という位置づけである。

### 9.3 MCSへの反映（SNRバックオフ → MCS再選択）

MCSはSNR閾値で選択されるため、マージンはSNRに適用する：

- snr_eff_db = snr_db - margin_db(mode)

この snr_eff_db に基づき MCS index を引き直し、推定レート（estimate）を得る。

- truth：`throughput_mbps_mcs`（既存：マージン無し）  
- estimate：`throughput_mbps_mcs_est`（新規：マージン適用）

さらに透明性のため、以下の列も出力する：

- `snr_db_eff_margin`（マージン適用後SNR）
- `mcs_index_est`（推定MCS）
- `margin_db_used`（適用マージン）

---

## 10. 最適化：opt列とeval列の分離（ベースライン vs 提案）

同一データに対し、以下の2条件を比較する。

### 10.1 Baseline
- 最適化入力（opt列）：`throughput_mbps_mcs`  
- 評価（eval列）：`throughput_mbps_mcs`

### 10.2 Proposed（mode-aware margin）
- 最適化入力（opt列）：`throughput_mbps_mcs_est`（マージン適用の推定値）  
- 評価（eval列）：`throughput_mbps_mcs`（真値）

これにより、改善が見えた場合には

> 「保守的見積りを用いることでリンク選択が変化し、真値評価におけるアウトエージが低減した」

と解釈できる。

### 10.3 評価指標

主指標（必須）：
- アウトエージ率：P(eval_throughput < T_out)
- 下位5%点（P05）：信頼性の改善が見えやすい

補助指標（任意）：
- 平均スループット（平均は下がっても良い）
- リンク選択比率（V2I/V2V）
- 選択変化率（baselineとproposedで選択が変わった割合）

---

## 11. 実験統計例（交差点シナリオ：条件別の観測）

交通密度調整後の交差点シナリオにおける条件別統計の例（あなたの集計出力より、抜粋）：

- All：count=180,887、距離中央値 51.3m、SNR中央値 22.7dB、MCS中央値 6
- LOS：count=150,038、距離中央値 42.1m、SNR中央値 24.6dB、MCS中央値 6
- NLOS：count=30,849、距離中央値 79.3m、SNR中央値 2.40dB、MCS中央値 2
- NLOS & D：count=27,072、SNR中央値 1.97dB、MCS中央値 2
- NLOS & K：count=3,777、SNR中央値 10.54dB、MCS中央値 4

このように、NLOS下でのレジーム差（D/K）が確認される。  
ただし、本研究の提案は「この1シナリオでDが弱かったからDを下げる」という経験則ではなく、  
**Rayleighの下振れを見込む理論マージン（margin_D）**を採用し、D/Kに応じてマージンを切り替えることで、アウトエージ回避を狙うものである。

---

## 12. 議論：意義と限界

### 12.1 意義
- Ray Tracingで建物遮蔽・反射を反映した環境依存リンク品質を生成可能。
- MCS（離散レート）によりアウトエージを現実寄りに定義可能。
- フェージング理論に基づく mode-aware margin により、D/Kを信頼性設計へ接続。
- **opt列とeval列の分離**により、パケットレベルを回さずとも最適化手法の効果（アウトエージ率低減）を客観評価可能。

### 12.2 限界
- MCSは研究用簡略モデルでありBLERターゲット等は含まない。
- HARQ/再送、MAC競合、制御オーバーヘッドは未モデル化。
- D/Kの閾値やK側マージンは設計パラメータであり汎化には追加検証が必要。

---

## 13. 今後の課題（論文での“次”として自然）

- 複数都市形状・BS配置での再現性評価（シナリオ汎化）
- `dominance` や `k_factor_db` を用いた連続的マージン（滑らかなリスクモデル）
- リレー可能率の閾値感度分析（T_out, T_v2v_min, T_v2i_good の簡易スイープ）
- より現実的なL2S（EESM等）への拡張（必要に応じて）

---

## 付録：実装整合メモ（論文とコードの対応）

- truth：`throughput_mbps_mcs`
- estimate：`throughput_mbps_mcs_est`
- 最適化CLI推奨：
  - `--opt-throughput-col throughput_mbps_mcs_est`
  - `--eval-throughput-col throughput_mbps_mcs`
  - `--outage-threshold-mbps 50`

以上
